[project]
name = "assignment-1-sale-price-data-ml-workflow"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyterlab>=4.6.3",
    "matplotlib>=3.11.2",
    "numpy>=2.4.6",
    "pandas>=3.0.5",
    "scikit-learn>=1.9.1",
]

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = []
